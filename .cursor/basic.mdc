# ????????

## Zustand ????

### Agents ????
- **??**: ?? agents ???????? Zustand store
?????
- **??**: ???? `/api/agents` API ??
- **????**: ?? `useAgentStore` ? `@/store/agents`

```typescript
import { useAgentStore } from '@/store/agents';

// ??????
const { agents, loading, error, fetchAgents, addAgent,
updateAgent, deleteAgent, getAgentById } =
useAgentStore();
```

### ????
- `fetchAgents()` - ???? agents
- `addAgent(agentData)` - ??? agent
- `updateAgent(id, agentData)` - ???? agent
- `deleteAgent(id)` - ???? agent
- `getAgentById(id)` - ?? ID ???? agent

### ????
- `agents` - agents ??
- `loading` - ????
- `error` - ????

### ????
1. ??????? `fetchAgents()` ????
2. ?? store ?? loading ? error ???? UI
3. ?? CRUD ???? store ????
4. ????? API ??????? store ??????